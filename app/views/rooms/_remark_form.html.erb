<%= form_with(model: remark, local: true) do |form| %>
  <% if remark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(remark.errors.count, "error") %> prohibited this remark from being saved:</h2>

      <ul>
      <% remark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :remark %>
    <%= form.text_area :text, size: "15x2", class: "form-control" %>
  </div>

  <div class="field">
    <%= form.hidden_field :room_id, value: remark.room_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>